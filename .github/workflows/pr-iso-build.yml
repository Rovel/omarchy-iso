name: PR ISO Build

on:
  workflow_dispatch:
  pull_request:

jobs:
  build-iso:
    runs-on: ubuntu-latest
    env:
      OMARCHY_INSTALLER_REPO: ${{ vars.OMARCHY_INSTALLER_REPO != '' && vars.OMARCHY_INSTALLER_REPO || 'basecamp/omarchy' }}
      OMARCHY_INSTALLER_REF: ${{ vars.OMARCHY_INSTALLER_REF != '' && vars.OMARCHY_INSTALLER_REF || 'master' }}
    container:
      image: archlinux/archlinux:latest
      options: --privileged
      volumes:
        - ${{ github.workspace }}:/workspace
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build ISO in Arch Linux container
      run: |
        pacman -Syu --noconfirm && \
        echo "Installing git..." && \
        pacman -S --noconfirm git && \
        echo "Running build-iso.sh script..." && \
        chmod +x /workspace/builder/build-iso.sh && \
        OMARCHY_INSTALLER_REPO="${OMARCHY_INSTALLER_REPO}" \
        OMARCHY_INSTALLER_REF="${OMARCHY_INSTALLER_REF}" \
        /workspace/builder/build-iso.sh
    
    - name: Upload ISO artifact
      uses: actions/upload-artifact@v4
      with:
        name: omarchy-iso-${{ github.run_number }}
        path: out/*.iso
        retention-days: 30
